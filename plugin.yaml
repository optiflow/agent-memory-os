name: meta_memory
version: 0.1.0
description: Local-first meta memory provider backed by the agent-memory-os TypeScript CLI.
provider_class: MetaMemoryProvider
provides_tools:
  - status
  - context_pack
  - add_relation
  - probe_relations
  - remember
  - search
  - upsert_session_state
  - upsert_resource
  - browse_resources
  - verify
provides_hooks:
  - on_session_end
